info:
  name: US04
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/productos/999
  body:
    type: json
    data: |-
      {
        "codigo": "TG-001",
        "descripcion": "Teclado mecánico RGB actualizado",
        "precio": 75000.0,
        "stock": 25
      }  
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
